@startuml

title 错误到退出码映射流程图

start
:执行 main()/installer.run;
note right
  例：install_state 写入阶段锁获取超时（TimeoutError）
  在 BaseInstaller.run 中被包装为 InstallError
end note

if (无异常?) then (yes)
  :返回 0;
  stop
else (no)
endif

if (异常是 ConfigError?) then (yes)
  :返回 10;
  stop
elseif (异常是 DownloadError?) then (yes)
  :返回 20;
  stop
elseif (异常是 SignatureVerifyError?) then (yes)
  :返回 40;
  stop
elseif (异常是 InstallError?) then (yes)
  :返回 50;
  stop
elseif (异常是 CleanupError?) then (yes)
  :返回 60;
  stop
else (other)
  :返回 1 (main() fallback);
  stop
endif

@enduml
